# Title

Split native opencode pair integration into child tickets

## Status

done

## Priority

high

## Type

process

## Summary

Break the native `opencode pair` integration work into smaller tickets before implementation starts.

## Scope

- Identify a safe implementation breakdown for `TICK-0011`
- Create child tickets for the first native integration milestones
- Update the parent ticket notes with child ticket references
- Refresh `tickets/ROADMAP.md` so active work points at the child tickets

## Out of Scope

- Implementing the native integration itself

## Acceptance Criteria

- `TICK-0011` references child tickets
- Child tickets exist with clear scopes
- `tickets/ROADMAP.md` prioritizes the child tickets instead of the oversized parent ticket

## Notes

- This split is required by the repository workflow before implementing the selected P4 ticket.
